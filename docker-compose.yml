version: '3.7'
services:
  dynamodb-local:
    image: amazon/dynamodb-local:latest
    container_name: dynamodb-local
    ports:
      - "8000:8000"
  dynamodb-test:
    image: amazon/dynamodb-local:latest
    container_name: dynamodb-test
    ports:
      - "8800:8800"